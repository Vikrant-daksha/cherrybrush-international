import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Allow Google Fonts (loaded via CSS @import in globals.css fallback)
  // next/font handles it natively via layout.tsx
};

export default nextConfig;
